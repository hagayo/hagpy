const t = (en, he) => ({ en, he });

export const referenceLessons = {
  'variables-types': {
    minutes: 32, prerequisites: ['syntax'],
    objectives: [t('Distinguish a value from a variable.', 'להבדיל בין ערך למשתנה.'), t('Use clear names and recognize common built-in types.', 'להשתמש בשמות ברורים ולזהות טיפוסים מובנים.'), t('Predict reassignment and simple conversions.', 'לחזות השמה חוזרת והמרות פשוטות.')],
    why: t('Programs handle names, scores, prices, and missing information. A variable gives each useful value a meaningful name, so later code can use it without repeating or guessing.', 'תוכניות מטפלות בשמות, ציונים, מחירים ומידע חסר. משתנה נותן לכל ערך שימושי שם משמעותי, כך שקוד בהמשך יכול להשתמש בו בלי לחזור עליו ובלי לנחש.'),
    plain: t('A value is the data itself: 24, "Maya", True, or None. A variable is a name that currently refers to a value. In Python, = is assignment: read it right to left. Python evaluates the value on the right, then makes the name on the left refer to it.', 'ערך הוא המידע עצמו: ‎24, ‏"Maya", ‏True או None. משתנה הוא שם שמפנה כרגע לערך. ב־Python הסימן = הוא השמה: קוראים אותו מימין לשמאל. Python מחשבת את הערך מימין ואז גורמת לשם משמאל להפנות אליו.'),
    terms: [['value', t('The data itself, such as 24 or "Maya".', 'המידע עצמו, למשל ‎24 או "Maya".')], ['variable', t('A meaningful name that refers to a value.', 'שם משמעותי שמפנה לערך.')], ['assignment', t('Putting a value behind a name with =.', 'הצמדת ערך לשם באמצעות =.')], ['type', t('The kind of value and its useful operations.', 'סוג הערך והפעולות המתאימות לו.')]],
    basic: { lead: t('Names sit on the left and values on the right. The example uses five common types before inspecting two of them.', 'השמות נמצאים משמאל והערכים מימין. הדוגמה משתמשת בחמישה טיפוסים נפוצים ואז בודקת שניים מהם.'), code: 'student_name = "Maya"\ncompleted_lessons = 4\nprogress = 0.5\nis_active = True\nnext_lesson = None\n\nprint(type(student_name))\nprint(type(progress))', lines: [t('student_name holds text, so its type is str.', 'student_name מחזיק טקסט, ולכן הטיפוס שלו הוא str.'), t('completed_lessons is a whole number: int; progress has a decimal point: float.', 'completed_lessons הוא מספר שלם: int; ל־progress יש נקודה עשרונית: float.'), t('is_active is a Boolean value, and None deliberately means “no value chosen yet”.', 'is_active הוא ערך בוליאני, ו־None מציין במכוון "עדיין לא נבחר ערך".'), t('type is an inspection tool: it reports a value’s type, not the value itself.', 'type הוא כלי בדיקה: הוא מחזיר את טיפוס הערך, ולא את הערך עצמו.')], output: t('The output is <class \'str\'> and <class \'float\'>.', 'הפלט הוא ‎<class \'str\'> ואחריו ‎<class \'float\'>.') },
    practical: { lead: t('A name can later refer to a different value. Text and numbers are separate types, so converting a number to text must be explicit.', 'שם יכול בהמשך להפנות לערך אחר. טקסט ומספרים הם טיפוסים נפרדים, ולכן המרה של מספר לטקסט צריכה להיות מפורשת.'), code: 'score = 82\nbonus = 5\nscore = score + bonus\n\nmessage = "Score: " + str(score)\nprint(message)', lines: [t('score starts as 82. Python adds bonus, then score refers to 87.', 'score מתחיל ב־82. Python מחברת את bonus, ואז score מפנה ל־87.'), t('str(score) converts the number to text so it can join the text message.', '‏str(score) ממירה את המספר לטקסט כך שאפשר לחבר אותו להודעה.')], output: t('It prints Score: 87. Writing "82" would create text, not a number.', 'הקוד מדפיס ‎Score: 87. כתיבה של "82" הייתה יוצרת טקסט ולא מספר.') },
    tryIt: t('Change bonus to -10. Predict the new score before running the program.', 'שנו את bonus ל־‎-10. חזו את הציון החדש לפני הרצת התוכנית.'),
    mistakes: [t('Using vague names like x when student_name or total_price says what the value means.', 'שימוש בשם עמום כמו x כאשר student_name או total_price מסבירים את משמעות הערך.'), t('Expecting "5" + 2 to work; convert deliberately with str or int.', 'ציפייה לכך ש־"5" + 2 יעבוד; יש להמיר במכוון באמצעות str או int.'), t('Using = for comparison. Use == when checking equality.', 'שימוש ב־= להשוואה. השתמשו ב־== לבדיקת שוויון.')],
    exercise: { prompt: t('Create city, temperature, and has_rain. Print one message containing all three values.', 'צרו city, temperature ו־has_rain. הדפיסו הודעה אחת שמכילה את שלושת הערכים.'), hints: [t('Write one assignment per line.', 'כתבו השמה אחת בכל שורה.'), t('An f-string can include values: f"{city}".', '‏f-string יכולה לשלב ערכים: ‎f"{city}".')], solution: 'city = "Haifa"\ntemperature = 27\nhas_rain = False\nprint(f"{city}: {temperature}°C, rain: {has_rain}")' },
    summary: [t('Values are data; variables are names for data.', 'ערכים הם מידע; משתנים הם שמות למידע.'), t('int, float, str, bool, and None represent different kinds of values.', 'int, float, str, bool ו־None מייצגים סוגים שונים של ערכים.'), t('A hint such as score: int = 82 documents the intended type.', 'רמז כגון ‎score: int = 82 מתעד את הטיפוס המתוכנן.')],
  },
  functions: {
    minutes: 36, prerequisites: ['variables-types', 'conditions'],
    objectives: [t('Define and call focused functions.', 'להגדיר ולקרוא לפונקציות ממוקדות.'), t('Tell parameters, arguments, return values, and print apart.', 'להבדיל בין פרמטרים, ארגומנטים, ערכים מוחזרים ו־print.'), t('Use defaults, keywords, scope, and type hints.', 'להשתמש בברירות מחדל, מילות מפתח, scope ורמזי טיפוס.')],
    why: t('Copying a calculation into several places makes later changes risky. A function gives one job a name and one home, then any caller can reuse its result.', 'העתקת חישוב לכמה מקומות הופכת שינויים בהמשך למסוכנים. פונקציה נותנת לתפקיד אחד שם ובית אחד, וכל קורא יכול להשתמש שוב בתוצאה.'),
    plain: t('def defines a function but does not run its indented body. A call runs it. A parameter is the name in the definition; an argument is the value supplied in a call. return sends data back to the caller. print only displays text for a person.', '‏def מגדירה פונקציה אך לא מריצה את הגוף המוזח שלה. קריאה מריצה אותה. פרמטר הוא השם בהגדרה; ארגומנט הוא הערך שמספקים בקריאה. ‏return מחזירה מידע לקוד הקורא. print רק מציגה טקסט לאדם.'),
    terms: [['function', t('Named code that does one job when called.', 'קוד בעל שם שמבצע תפקיד אחד כאשר קוראים לו.')], ['parameter', t('A named input in a definition.', 'קלט בעל שם בהגדרה.')], ['argument', t('The actual value passed in a call.', 'הערך בפועל שמועבר בקריאה.')], ['return value', t('The result sent back to the caller.', 'התוצאה שנשלחת בחזרה לקוד הקורא.')]],
    basic: { lead: t('This function receives a score and returns a label. Showing the label is deliberately a separate job.', 'הפונקציה מקבלת ציון ומחזירה תווית. הצגת התווית היא במכוון תפקיד נפרד.'), code: 'def score_label(score: int) -> str:\n    if score >= 60:\n        return "passed"\n    return "practice needed"\n\nlabel = score_label(72)\nprint(label)', lines: [t('score: int and -> str document intended input and output types.', '‏score: int ו־‎-> str מתעדים את טיפוסי הקלט והפלט המתוכננים.'), t('score is local to this call; the condition chooses a returned string.', 'score מקומי לקריאה הזו; התנאי בוחר מחרוזת מוחזרת.'), t('72 is the argument assigned to parameter score. print happens after the result returns.', '‏72 הוא הארגומנט שמוצמד לפרמטר score. ‏print מתבצעת לאחר החזרת התוצאה.')], output: t('The output is passed. Replacing return with print would leave label as None.', 'הפלט הוא passed. החלפת return ב־print הייתה משאירה את label כ־None.') },
    practical: { lead: t('A default makes an input optional when one choice is usually right. Keyword arguments make multi-value calls easy to read.', 'ברירת מחדל הופכת קלט לאופציונלי כאשר בחירה אחת מתאימה בדרך כלל. ארגומנטים בשם הופכים קריאה עם כמה ערכים לקריאה.'), code: 'def welcome(name: str, greeting: str = "Hello") -> str:\n    return f"{greeting}, {name}!"\n\nprint(welcome("Maya"))\nprint(welcome(name="Noa", greeting="Shalom"))', lines: [t('greeting has a default, so the first caller may omit it.', 'ל־greeting יש ברירת מחדל, ולכן הקריאה הראשונה יכולה להשמיט אותו.'), t('The second call names both arguments, making their roles explicit.', 'הקריאה השנייה נותנת שם לשני הארגומנטים, ולכן תפקידיהם מפורשים.')], output: t('It prints Hello, Maya! and Shalom, Noa!. name exists only while its call runs: local scope.', 'הפלט הוא ‎Hello, Maya! ואחריו ‎Shalom, Noa!. ‏name קיים רק בזמן שהקריאה רצה: scope מקומי.') },
    tryIt: t('Call welcome with greeting="Welcome". Then try to print name outside the function and read the NameError.', 'קראו ל־welcome עם ‎greeting="Welcome". אחר כך נסו להדפיס את name מחוץ לפונקציה וקראו את ה־NameError.'),
    mistakes: [t('Using print where another part of the program needs a reusable result.', 'שימוש ב־print כאשר חלק אחר בתוכנית זקוק לתוצאה שניתן להשתמש בה.'), t('Giving one function unrelated jobs; split calculation, validation, and display.', 'מתן תפקידים לא קשורים לפונקציה אחת; פצלו חישוב, אימות והצגה.'), t('Using a list or dictionary as a default value; the next lesson explains safe alternatives.', 'שימוש ברשימה או מילון כברירת מחדל; השיעור הבא מסביר חלופות בטוחות.')],
    exercise: { prompt: t('Write is_even(number: int) -> bool. Return True for an even number; do not print from the function.', 'כתבו ‎is_even(number: int) -> bool. החזירו True למספר זוגי; אל תדפיסו מתוך הפונקציה.'), hints: [t('Use the remainder operator %.', 'השתמשו באופרטור השארית %.'), t('number % 2 == 0 is already a Boolean value.', '‏number % 2 == 0 הוא כבר ערך בוליאני.')], solution: 'def is_even(number: int) -> bool:\n    return number % 2 == 0' },
    summary: [t('Focused functions take clear inputs and return results.', 'פונקציות ממוקדות מקבלות קלטים ברורים ומחזירות תוצאות.'), t('Parameters are in definitions; arguments are in calls.', 'פרמטרים נמצאים בהגדרות; ארגומנטים בקריאות.'), t('Prefer return for reusable data and keep scope small.', 'העדיפו return למידע שימושי ושמרו על scope קטן.')],
  },
  classes: {
    minutes: 40, prerequisites: ['functions', 'collections'],
    objectives: [t('Know when a class is useful and when it is not.', 'לדעת מתי מחלקה מועילה ומתי לא.'), t('Create objects with __init__, self, attributes, and methods.', 'ליצור אובייקטים עם __init__, ‏self, מאפיינים ומתודות.'), t('Protect valid state and meet composition.', 'להגן על מצב תקין ולהכיר composition.')],
    why: t('Use a class when a concept has state that changes over time and rules for changing it: an account, cart, or enrollment. A one-off calculation needs a function, and a record without behavior may be clearer as a dictionary or later a dataclass.', 'השתמשו במחלקה כאשר למושג יש מצב שמשתנה לאורך זמן וחוקים לשינוי שלו: חשבון, סל או הרשמה. חישוב חד־פעמי צריך פונקציה, ורשומה ללא התנהגות עשויה להיות ברורה יותר כמילון או בהמשך כ־dataclass.'),
    plain: t('A class is a blueprint. An object, also called an instance, is one concrete thing made from it. __init__ runs when Python creates an object and sets its starting attributes. self means this particular object; methods use it to read or change that object’s state.', 'מחלקה היא תבנית. אובייקט, שנקרא גם instance, הוא דבר ממשי אחד שנוצר ממנה. ‏__init__ רצה כאשר Python יוצרת אובייקט ומגדירה את המאפיינים ההתחלתיים שלו. ‏self פירושו האובייקט המסוים הזה; מתודות משתמשות בו כדי לקרוא או לשנות את המצב שלו.'),
    terms: [['class', t('A blueprint describing objects and behavior.', 'תבנית שמתארת אובייקטים והתנהגות.')], ['object / instance', t('One concrete value created from a class.', 'ערך ממשי שנוצר ממחלקה.')], ['attribute', t('Data stored on an object, such as self.balance.', 'מידע על אובייקט, למשל self.balance.')], ['method', t('A function on a class that works with an object.', 'פונקציה במחלקה שעובדת עם אובייקט.')]],
    basic: { lead: t('Every Enrollment has its own learner name and completed count. Calling a method changes only the object receiving the call.', 'לכל Enrollment יש שם לומד ומספר השלמות משלו. קריאה למתודה משנה רק את האובייקט שמקבל אותה.'), code: 'class Enrollment:\n    def __init__(self, learner_name: str) -> None:\n        self.learner_name = learner_name\n        self.completed_lessons = 0\n\n    def mark_complete(self) -> None:\n        self.completed_lessons += 1\n\nmaya = Enrollment("Maya")\nmaya.mark_complete()\nprint(maya.completed_lessons)', lines: [t('class defines a blueprint; no object exists until Enrollment("Maya").', 'class מגדירה תבנית; אובייקט לא קיים עד ‎Enrollment("Maya").'), t('__init__ stores initial attributes on self. mark_complete is behavior that changes this object.', '__init__ שומרת מאפיינים התחלתיים על self. ‏mark_complete היא התנהגות שמשנה את האובייקט הזה.'), t('Python passes maya as self automatically when maya.mark_complete() is called.', 'Python מעבירה את maya כ־self אוטומטית בקריאה ל־‎maya.mark_complete().')], output: t('The output is 1. A second Enrollment starts at 0 because objects own separate state.', 'הפלט הוא ‎1. Enrollment שנייה מתחילה ב־‎0 כי אובייקטים מחזיקים מצב נפרד.') },
    practical: { lead: t('Methods should guard the rules of the state they change. This account rejects an invalid deposit before its balance changes.', 'מתודות צריכות לשמור על החוקים של המצב שהן משנות. החשבון הזה דוחה הפקדה לא תקינה לפני שהיתרה משתנה.'), code: 'class Account:\n    def __init__(self, balance: int = 0) -> None:\n        self.balance = balance\n\n    def deposit(self, amount: int) -> None:\n        if amount <= 0:\n            raise ValueError("amount must be positive")\n        self.balance += amount\n\naccount = Account(100)\naccount.deposit(25)\nprint(account.balance)', lines: [t('The default gives a safe starting balance; callers can also supply one.', 'ברירת המחדל נותנת יתרת התחלה בטוחה; קוראים יכולים גם לספק יתרה.'), t('The guard stops invalid input before self.balance changes.', 'הבדיקה עוצרת קלט לא תקין לפני ש־self.balance משתנה.'), t('Only a valid deposit updates this account.', 'רק הפקדה תקינה מעדכנת את החשבון הזה.')], output: t('It prints 125. deposit(-25) raises ValueError and preserves the old balance.', 'הקוד מדפיס ‎125. ‏deposit(-25) מעלה ValueError ושומרת על היתרה הישנה.') },
    tryIt: t('Create a second Account with 50, deposit 10 only into it, and print both balances.', 'צרו Account שנייה עם ‎50, הפקידו ‎10 רק אליה והדפיסו את שתי היתרות.'),
    mistakes: [t('Creating a class merely to group unrelated functions; a module is often clearer.', 'יצירת מחלקה רק כדי לאגד פונקציות לא קשורות; מודול לרוב ברור יותר.'), t('Forgetting self or asking callers to pass it; Python passes the receiving object.', 'שכחת self או בקשה מהקוראים להעביר אותה; Python מעבירה את האובייקט המקבל.'), t('Letting outside code freely alter important state; validate near the changing method.', 'מתן אפשרות לקוד חיצוני לשנות מצב חשוב בחופשיות; יש לאמת קרוב למתודה שמשנה.')],
    exercise: { prompt: t('Create ReadingList. __init__ starts self.books as an empty list. add_book(title) adds only a non-empty title and otherwise raises ValueError.', 'צרו ReadingList. ‏__init__ מתחילה את self.books כרשימה ריקה. ‏add_book(title) מוסיפה רק כותרת שאינה ריקה ואחרת מעלה ValueError.'), hints: [t('Store the list on the object: self.books = [].', 'שמרו את הרשימה על האובייקט: ‎self.books = [].'), t('Check title before append.', 'בדקו את title לפני append.')], solution: 'class ReadingList:\n    def __init__(self) -> None:\n        self.books: list[str] = []\n\n    def add_book(self, title: str) -> None:\n        if not title:\n            raise ValueError("title is required")\n        self.books.append(title)' },
    summary: [t('A class joins meaningful state and behavior.', 'מחלקה מחברת מצב והתנהגות משמעותיים.'), t('__init__ establishes state and self identifies one object.', '__init__ קובעת מצב ו־self מזהה אובייקט אחד.'), t('Composition lets objects contain or use other objects and is often simpler than inheritance.', 'Composition מאפשרת לאובייקטים להכיל או להשתמש באובייקטים אחרים ולעיתים היא פשוטה יותר מירושה.')],
  },
  'docker-basics': {
    minutes: 42, prerequisites: ['uv', 'modules-packages'],
    objectives: [t('Explain what Docker packages and what it does not replace.', 'להסביר מה Docker אורזת ומה היא לא מחליפה.'), t('Distinguish a Dockerfile, an image, and a running container.', 'להבדיל בין Dockerfile, Image ו־Container רץ.'), t('Build and run a small FastAPI backend through Docker.', 'לבנות ולהריץ backend קטן של FastAPI באמצעות Docker.')],
    why: t('Code can work on one computer and fail on another because Python versions, installed packages, operating-system tools, or configuration differ. Docker puts the application and the runtime it needs into a repeatable image. The same image can run on a developer computer, a test server, or a cloud platform.', 'קוד יכול לעבוד במחשב אחד ולהיכשל באחר כי גרסת Python, החבילות המותקנות, כלי מערכת ההפעלה או ההגדרות שונים. Docker אורזת את האפליקציה ואת סביבת ההרצה שהיא צריכה לתוך Image שניתנת לשחזור. אותה Image יכולה לרוץ במחשב של מפתח, בשרת בדיקות או בפלטפורמת ענן.'),
    plain: t('Docker is a tool for running isolated application processes. You write a Dockerfile: a list of instructions for building an image. An image is a read-only package containing files and runtime setup. A container is one running process created from that image. A container is not a virtual machine: it shares the host operating system kernel and starts much faster.', 'Docker היא כלי להפעלת תהליכי אפליקציה מבודדים. כותבים Dockerfile: רשימת הוראות לבניית Image. ‏Image היא חבילה לקריאה בלבד שמכילה קבצים והגדרת סביבת הרצה. ‏Container היא תהליך רץ אחד שנוצר מאותה Image. ‏Container אינה מכונה וירטואלית: היא חולקת את ליבת מערכת ההפעלה של המארח ולכן עולה מהר יותר.'),
    visual: { src: '../assets/images/docker-containers.png', alt: t('Diagram showing an app, libraries, runtime, and configuration packaged into a container that runs the same on a developer machine, server, and cloud.', 'תרשים שמראה אפליקציה, ספריות, סביבת ריצה והגדרות שנארזות לקונטיינר ורצות אותו דבר במחשב מפתח, בשרת ובענן.'), caption: t('A container packages the application with the dependencies it needs, then runs the same way in each target environment.', 'קונטיינר אורז את האפליקציה עם התלויות שהיא צריכה, ואז רץ באותה צורה בכל סביבת יעד.') },
    terms: [['Dockerfile', t('A text file containing build instructions for an image.', 'קובץ טקסט שמכיל הוראות בנייה ל־Image.')], ['image', t('A reusable, immutable package built from a Dockerfile.', 'חבילה רב־פעמית שאינה משתנה ונבנית מ־Dockerfile.')], ['container', t('One running instance created from an image.', 'מופע רץ אחד שנוצר מ־Image.')], ['port mapping', t('A connection from a host port to a port inside a container.', 'חיבור בין פורט במחשב המארח לפורט בתוך הקונטיינר.')]],
    basic: { lead: t('Start with the smallest useful workflow. docker build reads the Dockerfile and creates an image. docker run starts a container from that image. -p 8000:8000 maps port 8000 on your computer to port 8000 inside the container.', 'מתחילים בתהליך הקטן והשימושי ביותר. ‏docker build קוראת את Dockerfile ויוצרת Image. ‏docker run מפעילה Container מאותה Image. ‏-p 8000:8000 ממפה את פורט ‎8000 במחשב שלכם לפורט ‎8000 בתוך הקונטיינר.'), code: 'docker build -t hagpy-api .\ndocker run --rm -p 8000:8000 hagpy-api', lines: [t('The final dot means “use the current folder as the build context”. Docker can copy only files from this context.', 'הנקודה בסוף פירושה "השתמשו בתיקייה הנוכחית כ־build context". ‏Docker יכולה להעתיק רק קבצים מתוך ההקשר הזה.'), t('-t gives the new image a memorable name: hagpy-api.', 'הדגל ‎-t נותן ל־Image החדשה שם ברור: ‎hagpy-api.'), t('--rm removes the stopped container automatically; it does not delete the image.', '‏--rm מוחק את ה־Container לאחר שהיא נעצרת; הוא לא מוחק את ה־Image.'), t('The first 8000 is the host port. The second is the port where the FastAPI process listens inside the container.', 'ה־‎8000 הראשון הוא הפורט במחשב המארח. השני הוא הפורט שבו תהליך FastAPI מאזין בתוך הקונטיינר.')], output: t('After the server starts, open http://localhost:8000/docs. FastAPI serves its interactive API documentation from inside the container.', 'לאחר שהשרת עולה, פתחו ‎http://localhost:8000/docs. ‏FastAPI מגישה את תיעוד ה־API האינטראקטיבי שלה מתוך הקונטיינר.') },
    practical: { lead: t('This Dockerfile is a production-minded starting point for a small FastAPI backend managed by uv. It copies the dependency files first so Docker can reuse that expensive build layer when only application code changes.', 'ה־Dockerfile הזו היא נקודת פתיחה שמותאמת לפרודקשן עבור backend קטן של FastAPI שמנוהל על ידי uv. היא מעתיקה קודם את קובצי התלויות, כדי ש־Docker תוכל להשתמש מחדש בשכבת הבנייה היקרה כאשר רק קוד האפליקציה משתנה.'), code: 'FROM python:3.12-slim\n\nWORKDIR /app\n\nCOPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev\n\nCOPY src ./src\n\nEXPOSE 8000\nCMD ["uv", "run", "fastapi", "run", "src/main.py", "--host", "0.0.0.0", "--port", "8000"]', lines: [t('python:3.12-slim supplies a small Python runtime. Pin a specific image version in a real deployment policy.', '‏python:3.12-slim מספקת סביבת Python קטנה. בפרודקשן אמיתי קובעים מדיניות עם גרסת Image מדויקת.'), t('WORKDIR makes /app the current folder for the following commands.', '‏WORKDIR הופכת את ‎/app לתיקייה הנוכחית עבור הפקודות הבאות.'), t('uv is copied into the image, then pyproject.toml and uv.lock are copied before source code.', '‏uv מועתקת ל־Image, ואז pyproject.toml ו־uv.lock מועתקים לפני קוד המקור.'), t('uv sync --frozen installs exactly what uv.lock describes and fails if the lockfile is out of date.', '‏uv sync --frozen מתקינה בדיוק את מה ש־uv.lock מתאר ומכשילה את הבנייה אם קובץ הנעילה אינו מעודכן.'), t('The application source is copied only after dependencies, so dependency installation can be cached.', 'קוד המקור מועתק רק אחרי התלויות, ולכן אפשר לשמור cache של התקנת התלויות.'), t('0.0.0.0 is essential: it lets Docker forward traffic into the container. 127.0.0.1 would listen only inside it.', '‏0.0.0.0 חיוני: הוא מאפשר ל־Docker להעביר תעבורה לתוך הקונטיינר. ‏127.0.0.1 היה מאזין רק בתוכה.')], output: t('Build this file from a project that contains pyproject.toml, uv.lock, and src/main.py. The container then runs FastAPI on port 8000.', 'בנו את הקובץ הזה מפרויקט שמכיל pyproject.toml,‏ uv.lock ו־src/main.py. הקונטיינר תריץ את FastAPI בפורט ‎8000.') },
    tryIt: t('Add a .dockerignore file containing .venv, __pycache__, .git, and .env. Build again and notice that local environments, caches, history, and secrets are no longer sent to Docker.', 'הוסיפו קובץ ‎.dockerignore שמכיל ‎.venv, ‏__pycache__, ‏.git ו־.env. בנו שוב ושימו לב שסביבות מקומיות, cache, היסטוריית Git וסודות כבר לא נשלחים ל־Docker.'),
    mistakes: [t('Treating an image as a running server. An image is the package; a container is the running process.', 'התייחסות ל־Image כאל שרת רץ. Image היא החבילה; Container היא התהליך הרץ.'), t('Copying .env or secrets into an image. Supply secrets through the deployment platform or a secure runtime configuration.', 'העתקת ‎.env או סודות לתוך Image. ספקו סודות דרך פלטפורמת הפריסה או הגדרות ריצה מאובטחות.'), t('Binding FastAPI to 127.0.0.1 inside the container. Use 0.0.0.0 so the port mapping can reach it.', 'קשירת FastAPI ל־127.0.0.1 בתוך הקונטיינר. השתמשו ב־0.0.0.0 כדי שמיפוי הפורטים יוכל להגיע אליה.')],
    exercise: { prompt: t('Create a .dockerignore for a Python API. Exclude the virtual environment, Python cache folders, Git history, environment files, and test coverage output.', 'צרו ‎.dockerignore עבור API של Python. החריגו את הסביבה הווירטואלית, תיקיות cache של Python, היסטוריית Git, קובצי סביבה ופלט כיסוי בדיקות.'), hints: [t('Each ignored path belongs on its own line.', 'כל נתיב שמחריגים נמצא בשורה משלו.'), t('Start with .venv, __pycache__, .git, .env, and htmlcov.', 'התחילו עם ‎.venv, ‏__pycache__, ‏.git, ‏.env ו־htmlcov.')], solution: '.venv\n__pycache__/\n*.pyc\n.git/\n.env\nhtmlcov/\n.coverage' },
    summary: [t('A Dockerfile builds an image; docker run creates a container from that image.', 'Dockerfile בונה Image; ‏docker run יוצרת Container מאותה Image.'), t('Port mapping exposes a service in a container to your computer or deployment environment.', 'מיפוי פורטים חושף שירות שבקונטיינר למחשב שלכם או לסביבת הפריסה.'), t('Copy lockfiles before source code, use --frozen, and never build secrets into an image.', 'העתיקו קובצי נעילה לפני קוד המקור, השתמשו ב־--frozen ולעולם אל תבנו סודות לתוך Image.')],
  },
};
