export const productGuidesInfra={
  'databases-overview':guide(
    'Memory ends when the process ends','הזיכרון נגמר כשהתהליך מסתיים',
    'A database stores durable application state and supports safe concurrent access. Choosing one starts from data relationships and queries, not popularity.','מסד נתונים שומר מצב מתמשך של האפליקציה ותומך בגישה מקבילית בטוחה. הבחירה מתחילה בקשרים ובשאילתות, לא בפופולריות.',[
      section('Why a list or JSON file eventually fails','למה List או קובץ JSON נשברים בהמשך','An in-memory list disappears on restart. A JSON file can survive restart, but concurrent requests can overwrite each other, searching becomes slow, and partial writes can corrupt the file. It is useful as a learning repository, not a production database.','רשימה בזיכרון נעלמת ב-Restart. קובץ JSON יכול לשרוד, אבל בקשות מקבילות עלולות לדרוס זו את זו, החיפוש נעשה איטי וכתיבה חלקית עלולה להשחית את הקובץ. הוא שימושי כ-Repository לימודי, לא כמסד Production.','request A: read file\nrequest B: read file\nrequest A: write version A\nrequest B: write version B  # A may be lost','race condition'),
      section('Three different roles','שלושה תפקידים שונים','PostgreSQL is a relational source of truth. MongoDB is a document database with flexible nested records. Redis is primarily an in-memory data structure server often used for cache, short-lived state and coordination. Redis should not automatically replace the durable database.','PostgreSQL הוא מקור אמת רלציוני. MongoDB הוא מסד Documents עם רשומות מקוננות גמישות. Redis הוא בעיקר שרת מבני נתונים בזיכרון שמשמש לעיתים קרובות ל-Cache, מצב קצר-חיים ותיאום. Redis אינו מחליף אוטומטית את המסד המתמשך.','PostgreSQL → tables, relations, transactions\nMongoDB   → documents, embedding, flexible shape\nRedis     → cache, TTL, counters, short-lived state','comparison'),
      section('Choose from access patterns','בוחרים לפי דפוסי גישה','Ask what must be unique, which records relate, which updates must succeed together, and which queries dominate. The repository contract lets the application delay the infrastructure choice.','שאלו מה חייב להיות ייחודי, אילו רשומות קשורות, אילו עדכונים חייבים להצליח יחד ואילו שאילתות נפוצות. חוזה ה-Repository מאפשר למערכת לדחות את בחירת התשתית.','Need strong relations and transactions? → PostgreSQL\nNeed document-shaped aggregates? → consider MongoDB\nNeed repeated reads faster? → add Redis after measuring','decision questions')],
    ['Do not choose NoSQL because it has no schema - the data still has a shape','Do not add Redis before identifying a slow repeated operation','Do not let database-specific types leak through every layer'],['אל תבחרו NoSQL כי אין לו Schema - למידע עדיין יש מבנה','אל תוסיפו Redis לפני שמזהים פעולה חוזרת ואיטית','אל תתנו לטיפוסים ספציפיים למסד לדלוף לכל שכבה'],
    'For dreams, steps and users, list the relationships, required unique values and two queries. Choose a primary database and defend the choice.','עבור Dreams, ‏Steps ו-Users, רשמו קשרים, ערכים ייחודיים ושתי שאילתות. בחרו מסד ראשי והסבירו את הבחירה.'),

  'mongodb-basics':guide(
    'Store related data as documents when it belongs together','שומרים מידע קשור כ-Documents כשהוא שייך יחד',
    'MongoDB stores BSON documents in collections. Documents resemble JSON but support additional types such as ObjectId and dates. Flexible schema means controlled evolution, not absence of validation.','MongoDB שומר BSON Documents בתוך Collections. ‏Documents דומים ל-JSON אך תומכים בטיפוסים נוספים כמו ObjectId ותאריכים. Schema גמיש פירושו התפתחות מבוקרת, לא היעדר Validation.',[
      section('Database, collection and document','Database, ‏Collection ו-Document','A database groups collections. A collection groups documents of one application concept. Each document has an _id and may contain nested objects and arrays.','Database מאגד Collections. ‏Collection מאגד Documents של מושג אחד במערכת. לכל Document יש ‎_id והוא יכול להכיל אובייקטים ומערכים מקוננים.','{\n  "_id": ObjectId("..."),\n  "title": "Learn FastAPI",\n  "status": "in_progress",\n  "steps": [\n    {"title": "Create route", "done": true},\n    {"title": "Write tests", "done": false}\n  ]\n}','dreams document'),
      section('Embed or reference','Embedding או Reference','Embed steps when they are owned by one dream, loaded together and remain reasonably bounded. Store references when related data grows independently, is shared, or requires separate access patterns.','הטמיעו Steps כאשר הם שייכים לחלום אחד, נטענים יחד ונשארים בגודל סביר. שמרו References כאשר המידע הקשור גדל עצמאית, משותף או דורש שאילתות נפרדות.','Embedded: dream contains its steps\nReferenced: dream contains owner_id that points to users','model choice'),
      section('Flexible does not mean inconsistent','גמיש אינו אומר לא עקבי','Pydantic validates API input, while database validation and migrations or update scripts manage stored documents. Old documents may not contain fields introduced later, so reads need intentional defaults.','Pydantic מאמת קלט API, בעוד Database Validation וסקריפטי Migration או Update מנהלים Documents שכבר נשמרו. Documents ישנים עשויים לא להכיל שדות שנוספו מאוחר יותר, ולכן הקריאה דורשת ברירות מחדל מכוונות.','status = document.get("status", "pending")\nversion = document.get("schema_version", 1)','repository mapping'),
      section('Indexes trade write cost for read speed','Indexes מחליפים עלות כתיבה במהירות קריאה','An index can avoid scanning every document for common filters, but each write must also update indexes. Add indexes for measured query patterns, including uniqueness where required.','Index יכול למנוע סריקה של כל Document עבור Filters נפוצים, אבל כל כתיבה צריכה לעדכן גם את ה-Indexes. הוסיפו Indexes עבור דפוסי שאילתה אמיתיים, כולל Uniqueness כשנדרש.','db.dreams.createIndex({ owner_id: 1, status: 1 })\ndb.dreams.createIndex({ owner_id: 1, title: 1 }, { unique: true })','mongosh')],
    ['Do not create one giant document that grows forever','Do not assume every document automatically follows the Pydantic model','Do not expose ObjectId directly without an API serialization decision'],['אל תיצרו Document ענקי שגדל לנצח','אל תניחו שכל Document מתאים אוטומטית למודל Pydantic','אל תחשפו ObjectId בלי החלטת Serialization ב-API'],
    'Design a MongoDB dream document. Decide whether steps are embedded and define two indexes based on real list and lookup queries.','תכננו Dream Document ב-MongoDB. החליטו אם Steps מוטמעים והגדירו שני Indexes לפי שאילתות List ו-Lookup אמיתיות.'),

  'postgresql-basics':guide(
    'Relationships and guarantees are first-class','קשרים והבטחות הם חלק מרכזי',
    'PostgreSQL stores data in typed tables and uses SQL. Primary keys identify rows, foreign keys protect relationships, constraints reject invalid state, and transactions group changes atomically.','PostgreSQL שומר מידע בטבלאות עם טיפוסים ומשתמש ב-SQL. ‏Primary Keys מזהים שורות, ‏Foreign Keys מגינים על קשרים, Constraints דוחים מצב לא תקין ו-Transactions מאגדות שינויים באופן אטומי.',[
      section('Model tables around entities','מתכננים טבלאות סביב ישויות','A dream and its steps have a one-to-many relationship. Keeping steps in their own table allows filtering, ordering and updating one step without rewriting a whole document.','לחלום ולשלבים שלו יש קשר של אחד-לרבים. שמירת Steps בטבלה נפרדת מאפשרת סינון, מיון ועדכון של שלב אחד בלי לכתוב מחדש Document שלם.','CREATE TABLE dreams (\n  id UUID PRIMARY KEY,\n  title VARCHAR(80) NOT NULL,\n  status VARCHAR(20) NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE TABLE dream_steps (\n  id UUID PRIMARY KEY,\n  dream_id UUID NOT NULL REFERENCES dreams(id) ON DELETE CASCADE,\n  title VARCHAR(120) NOT NULL,\n  position INTEGER NOT NULL\n);','schema.sql'),
      section('Constraints protect every writer','Constraints מגינים מול כל כותב','Pydantic protects the API path, but data can also arrive from scripts, migrations or other services. Database constraints remain the final durable guard.','Pydantic מגן על נתיב ה-API, אך מידע יכול להגיע גם מסקריפטים, Migrations או Services אחרים. Constraints במסד נשארים שכבת ההגנה המתמשכת האחרונה.','ALTER TABLE dreams\nADD CONSTRAINT valid_dream_status\nCHECK (status IN (\'pending\', \'in_progress\', \'done\'));\n\nCREATE UNIQUE INDEX unique_owner_title\nON dreams(owner_id, lower(title));','constraints.sql'),
      section('Transactions mean all or nothing','Transaction פירושו הכול או כלום','Creating a dream and its initial steps is one business change. If step insertion fails, the dream should not remain half-created. A transaction commits all changes or rolls them back.','יצירת חלום והשלבים הראשונים שלו היא שינוי עסקי אחד. אם הכנסת Step נכשלת, החלום לא צריך להישאר חצי-נוצר. Transaction מבצע Commit לכל השינויים או Rollback לכולם.','BEGIN;\nINSERT INTO dreams (...) VALUES (...);\nINSERT INTO dream_steps (...) VALUES (...);\nCOMMIT;\n-- Any failure before COMMIT should ROLLBACK','transaction.sql'),
      section('Indexes follow queries','Indexes נבנים לפי שאילתות','Primary keys are indexed, but list pages often need other indexes. An index on owner_id and created_at can serve the common query for one user’s newest dreams.','Primary Keys מקבלים Index, אך עמודי רשימה דורשים לעיתים Indexes נוספים. Index על owner_id ו-created_at יכול לשרת שאילתה נפוצה של החלומות החדשים למשתמש.','CREATE INDEX dreams_owner_created_idx\nON dreams(owner_id, created_at DESC);','indexes.sql')],
    ['Do not store comma-separated IDs in one text column','Do not catch an error and forget to rollback the session','Do not add indexes to every column without query evidence'],['אל תשמרו רשימת מזהים כמחרוזת אחת','אל תתפסו שגיאה ותשכחו לבצע Rollback ל-Session','אל תוסיפו Index לכל עמודה בלי ראיות משאילתה'],
    'Design dreams and steps tables with keys, constraints and one useful list index. Describe one operation that must use a transaction.','תכננו טבלאות Dreams ו-Steps עם מפתחות, Constraints ו-Index שימושי לרשימה. תארו פעולה אחת שחייבת להשתמש ב-Transaction.'),

  'redis-cache':guide(
    'A cache is a disposable copy, not the source of truth','Cache הוא עותק שניתן למחוק, לא מקור האמת',
    'Redis keeps data in memory and offers fast data structures. In the cache-aside pattern, PostgreSQL or MongoDB remains authoritative and Redis stores a temporary serialized result.','Redis שומר מידע בזיכרון ומציע מבני נתונים מהירים. בדפוס Cache-aside, ‏PostgreSQL או MongoDB נשאר מקור האמת ו-Redis שומר תוצאה זמנית שעברה Serialization.',[
      section('Cache hit and cache miss','Cache Hit ו-Cache Miss','Read Redis first. A hit returns immediately. On a miss, read the database, serialize the result and cache it with a TTL before returning it.','קוראים קודם מ-Redis. ‏Hit מחזיר מיד. ב-Miss קוראים מהמסד, מבצעים Serialization לתוצאה ושומרים אותה עם TTL לפני שמחזירים.','async def get_dream(dream_id: UUID) -> Dream:\n    key = f"dream:{dream_id}"\n    cached = await redis.get(key)\n    if cached is not None:\n        return Dream.model_validate_json(cached)\n\n    dream = await repository.get(dream_id)\n    await redis.set(key, dream.model_dump_json(), ex=60)\n    return dream','cached_repository.py'),
      section('TTL limits staleness','TTL מגביל מידע ישן','A time-to-live removes an entry automatically. It is a safety net, not a complete invalidation strategy. Shorter TTL reduces staleness but increases database reads.','Time-to-live מוחק Entry אוטומטית. זו רשת ביטחון, לא אסטרטגיית Invalidation מלאה. TTL קצר מפחית מידע ישן אך מגדיל קריאות למסד.','SETEX dream:73f2 60 "{...json...}"\nTTL dream:73f2','redis-cli'),
      section('Invalidate after successful writes','מוחקים Cache לאחר כתיבה מוצלחת','After the database transaction commits, delete affected cache keys. Deleting before a failed database write can cause unnecessary misses; forgetting deletion can return stale data.','לאחר שה-Transaction במסד מבצע Commit, מחקו את מפתחות ה-Cache שהושפעו. מחיקה לפני כתיבה שנכשלה גורמת ל-Misses מיותרים; שכחת מחיקה מחזירה מידע ישן.','await repository.update(dream)\nawait redis.delete(f"dream:{dream.id}")\nawait redis.delete(f"dreams:user:{dream.owner_id}")','dream_service.py'),
      section('Failure should often degrade, not destroy','כשל Cache צריך לרוב להאט, לא להשבית','For ordinary caching, if Redis is unavailable the service can log the failure and read the primary database. Whether to fail open depends on what Redis is used for; locks and rate limits have different safety requirements.','ב-Cache רגיל, אם Redis אינו זמין ה-Service יכול לתעד את הכשל ולקרוא מהמסד הראשי. ההחלטה אם להמשיך תלויה בשימוש: Locks ו-Rate Limits דורשים שיקולי בטיחות אחרים.')],
    ['Do not cache secrets or user-specific responses under shared keys','Do not assume Redis persistence makes cache data authoritative','Do not retry Redis forever inside a user request'],['אל תשמרו Secrets או תגובות אישיות תחת מפתח משותף','אל תניחו ש-Redis Persistence הופך Cache למקור אמת','אל תבצעו Retry ל-Redis לנצח בתוך בקשת משתמש'],
    'Design keys, TTL and invalidation for GET /dreams/{id} and GET /users/{id}/dreams. Explain behavior when Redis is unavailable.','תכננו Keys, ‏TTL ו-Invalidation עבור GET /dreams/{id} ו-GET /users/{id}/dreams. הסבירו מה קורה כש-Redis אינו זמין.'),

  'docker-basics':guide(
    'Package the runtime, not only the code','אורזים את סביבת הריצה, לא רק את הקוד',
    'A container image bundles the application filesystem, runtime and dependencies. Running the image creates an isolated process with predictable inputs. It reduces environment drift but does not remove configuration, security or operations.','Container Image מאגד את קובצי המערכת של האפליקציה, סביבת הריצה והתלויות. הרצת ה-Image יוצרת תהליך מבודד עם קלט צפוי. הדבר מפחית הבדלים בין סביבות אך אינו מבטל הגדרות, אבטחה או תפעול.',[
      section('Image, container and registry','Image, ‏Container ו-Registry','A Dockerfile is a recipe. docker build produces an immutable image. docker run starts a container from that image. A registry stores images so CI and cloud platforms can pull them.','Dockerfile הוא מתכון. docker build יוצר Image קבוע. docker run מפעיל Container מתוך ה-Image. ‏Registry שומר Images כדי ש-CI ופלטפורמות ענן יוכלו למשוך אותם.','Dockerfile --build--> Image --run--> Container\n                         |\n                         +--push--> Registry','container lifecycle'),
      section('Containers are isolated processes','Containers הם תהליכים מבודדים','A container shares the host kernel; it is not a full virtual machine. Files written inside its writable layer may disappear when the container is replaced, so durable data belongs in a database or mounted volume.','Container משתף את Kernel המארח; הוא אינו מכונה וירטואלית מלאה. קבצים שנכתבים לשכבה הניתנת לשינוי עלולים להיעלם כשה-Container מוחלף, ולכן מידע מתמשך שייך למסד או Volume.','docker run --rm python:3.12-slim python --version','PowerShell'),
      section('Layers make rebuilds faster','Layers מאיצות Builds חוזרים','Docker caches unchanged build steps. Copy dependency files and install dependencies before copying frequently changing source code, so a code edit does not reinstall everything.','Docker שומר ב-Cache שלבי Build שלא השתנו. העתיקו קובצי תלויות והתקינו אותן לפני קוד שמשתנה לעיתים קרובות, כדי שעריכת קוד לא תתקין הכול מחדש.','COPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev\nCOPY src ./src','Dockerfile order'),
      section('.dockerignore controls build context','‎.dockerignore שולט ב-Build Context','The build context is sent to the Docker daemon. Exclude Git history, virtual environments, caches, tests when unnecessary and every secret file. .gitignore does not automatically apply to Docker.','ה-Build Context נשלח ל-Docker. החריגו היסטוריית Git, סביבות וירטואליות, Caches, בדיקות כשאינן נדרשות וכל קובץ Secret. ‏.gitignore אינו חל אוטומטית על Docker.','.git\n.venv\n__pycache__\n.pytest_cache\n.env\nhtmlcov','.dockerignore')],
    ['Do not put a database inside the same application container','Do not bake .env or API keys into an image','Do not use latest tags when reproducibility matters'],['אל תכניסו מסד לאותו Application Container','אל תכניסו ‎.env או API Keys ל-Image','אל תשתמשו ב-latest כאשר Reproducibility חשובה'],
    'Explain what survives when a container is deleted: source copied into the image, environment variables, a file written at runtime and data in external PostgreSQL.','הסבירו מה שורד כאשר Container נמחק: קוד שהועתק ל-Image, משתני סביבה, קובץ שנכתב בזמן ריצה ומידע ב-PostgreSQL חיצוני.'),

  'docker-fastapi':guide(
    'One FastAPI image should run the same command everywhere','Image אחד של FastAPI צריך להריץ אותה פקודה בכל מקום',
    'The container must install locked dependencies, copy only required files, listen on every interface and receive configuration at runtime.','ה-Container צריך להתקין תלויות נעולות, להעתיק רק קבצים נדרשים, להאזין בכל Interface ולקבל הגדרות בזמן הריצה.',[
      section('A uv-based Dockerfile','Dockerfile המבוסס על uv','Use a small Python base image and copy a pinned uv binary from its official image. Install from uv.lock before source code to preserve the dependency layer cache.','השתמשו ב-Python Base Image קטן והעתיקו גרסה קבועה של uv מה-Image הרשמי שלו. התקינו מתוך uv.lock לפני קוד המקור כדי לשמור את Cache שכבת התלויות.','FROM python:3.12-slim\n\nCOPY --from=ghcr.io/astral-sh/uv:0.11.28 /uv /uvx /bin/\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev --no-install-project\n\nCOPY src ./src\n\nENV PATH="/app/.venv/bin:$PATH"\nCMD ["fastapi", "run", "src/dreamly/main.py", "--host", "0.0.0.0", "--port", "8000"]','Dockerfile'),
      section('Why 0.0.0.0 matters','למה ‎0.0.0.0 חשוב','Inside a container, localhost refers to the container itself. Binding only to 127.0.0.1 makes the server unreachable through Docker port publishing. 0.0.0.0 listens on all container interfaces.','בתוך Container, ‏localhost מתייחס ל-Container עצמו. האזנה רק ל-127.0.0.1 הופכת את השרת לבלתי נגיש דרך פרסום Ports של Docker. ‏0.0.0.0 מאזין בכל Interfaces של ה-Container.','docker build -t dreamly-api .\ndocker run --rm -p 8000:8000 --env-file .env dreamly-api\n# Open http://localhost:8000/docs','PowerShell'),
      section('Configuration enters at runtime','הגדרות נכנסות בזמן ריצה','The image should be reusable across development and production. Pass environment values when starting the container or through the cloud platform secret manager, never with COPY .env.','ה-Image צריך להיות ניתן לשימוש בפיתוח וב-Production. העבירו ערכי סביבה בהפעלת ה-Container או דרך Secret Manager בענן, ולעולם לא באמצעות COPY .env.','docker run --rm -p 8000:8000 `\n  -e APP_ENV=development `\n  -e DATABASE_URL=sqlite:///./data.db `\n  dreamly-api','PowerShell'),
      section('Stop correctly','עוצרים נכון','Container platforms send SIGTERM before stopping a process. Let the server shut down, close connections and stop accepting work. Do not swallow shutdown errors or rely on local files being preserved.','פלטפורמות Containers שולחות SIGTERM לפני עצירת תהליך. אפשרו לשרת להיסגר, לסגור Connections ולהפסיק לקבל עבודה. אל תבלעו שגיאות Shutdown ואל תסמכו על שימור קבצים מקומיים.')],
    ['Do not run development --reload in production','Do not expose a port and forget to bind the application to it','Do not copy the whole repository before dependency installation'],['אל תריצו ‎--reload של פיתוח ב-Production','אל תחשפו Port ותשכחו לקשור אליו את האפליקציה','אל תעתיקו את כל ה-Repository לפני התקנת תלויות'],
    'Build the image, run it, open /docs, stop it and run it again. Verify that code is present but an unmounted runtime file is not durable.','בנו את ה-Image, הריצו אותו, פתחו ‎/docs, עצרו והריצו שוב. ודאו שהקוד קיים אבל קובץ Runtime שלא נשמר ב-Volume אינו מתמשך.'),

  'cicd-overview':guide(
    'Automation turns a checklist into a gate','אוטומציה הופכת Checklist לשער',
    'CI checks every change in a clean environment. Continuous Delivery keeps a verified release ready. Continuous Deployment automatically publishes every change that passes the defined gates.','CI בודק כל שינוי בסביבה נקייה. Continuous Delivery שומר גרסה מאומתת מוכנה לשחרור. Continuous Deployment מפרסם אוטומטית כל שינוי שעבר את השערים שהוגדרו.',[
      section('Continuous Integration','Continuous Integration','Developers merge small changes frequently. Each push or pull request installs dependencies, checks formatting and linting, runs tests and reports one visible result.','מפתחים ממזגים שינויים קטנים לעיתים קרובות. כל Push או Pull Request מתקין תלויות, בודק Formatting ו-Linting, מריץ בדיקות ומדווח תוצאה גלויה אחת.','push / pull request\n  → uv sync --frozen\n  → ruff check\n  → pylint\n  → pytest + coverage\n  → pass or fail','CI pipeline'),
      section('Delivery versus Deployment','Delivery מול Deployment','With Continuous Delivery, a human chooses when to release the verified artifact. With Continuous Deployment, a successful pipeline deploys automatically. The difference is the release decision, not whether tests exist.','ב-Continuous Delivery אדם בוחר מתי לשחרר את ה-Artifact המאומת. ב-Continuous Deployment Pipeline מצליח מפרסם אוטומטית. ההבדל הוא בהחלטת השחרור, לא בעצם קיום הבדיקות.','Delivery: checks → build artifact → manual release\nDeployment: checks → build artifact → automatic release','release models'),
      section('Build once, promote the same artifact','בונים פעם אחת ומקדמים אותו Artifact','A Docker image should be built once, identified by an immutable commit SHA and promoted. Rebuilding separately for production can produce different dependencies or bytes than the tested image.','Docker Image צריך להיבנות פעם אחת, להיות מזוהה באמצעות Commit SHA קבוע ולהתקדם בין סביבות. Build חדש עבור Production עלול לייצר תלויות או Bytes שונים מה-Image שנבדק.','dreamly-api:sha-8f31c2a\nCI tests this image\nCD deploys this exact image','artifact identity'),
      section('A pipeline failure is a feature','כשל Pipeline הוא תכונה','A red check prevents an unverified change from reaching users. Do not bypass it to make the dashboard green; fix the code, test or pipeline configuration and preserve the evidence.','Check אדום מונע משינוי שלא אומת להגיע למשתמשים. אל תעקפו אותו כדי לצבוע את המסך בירוק; תקנו את הקוד, הבדיקה או הגדרת ה-Pipeline ושמרו את הראיות.')],
    ['Do not deploy before tests finish','Do not store cloud credentials in workflow YAML','Do not allow main to ignore required failing checks'],['אל תפרסמו לפני שהבדיקות הסתיימו','אל תשמרו Credentials לענן בתוך YAML','אל תאפשרו ל-main להתעלם מבדיקות חובה שנכשלו'],
    'Classify these as CI, Delivery or Deployment: PR tests, building an image, manual production approval, automatic deploy from main, and smoke test after deploy.','סווגו כ-CI, ‏Delivery או Deployment: בדיקות PR, בניית Image, אישור ידני ל-Production, פרסום אוטומטי מ-main ו-Smoke Test לאחר פרסום.'),

  'github-actions-cicd':guide(
    'A workflow is code that describes automation','Workflow הוא קוד שמתאר אוטומציה',
    'GitHub Actions reads YAML files under .github/workflows. Events start workflow runs, jobs receive fresh runners, and steps execute actions or shell commands.','GitHub Actions קורא קובצי YAML תחת ‎.github/workflows. אירועים מפעילים Workflow Runs, ‏Jobs מקבלים Runners נקיים ו-Steps מריצים Actions או פקודות Shell.',[
      section('Workflow anatomy','מבנה Workflow','The name is visible in GitHub. on defines triggers. jobs can run independently or depend on one another. Each job declares a runner and ordered steps.','השם נראה ב-GitHub. ‏on מגדיר Triggers. ‏Jobs יכולים לרוץ בנפרד או להיות תלויים זה בזה. כל Job מגדיר Runner ו-Steps לפי סדר.','name: CI\n\non:\n  pull_request:\n  push:\n    branches: [main]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4','ci.yml'),
      section('Install exactly what the project declares','מתקינים בדיוק את מה שהפרויקט מגדיר','Use setup-uv and the committed uv.lock. --frozen makes CI fail if project metadata and lockfile disagree instead of silently changing dependencies.','השתמשו ב-setup-uv וב-uv.lock שנשמר ב-Git. האפשרות ‎--frozen גורמת ל-CI להיכשל אם Metadata ו-Lockfile אינם תואמים במקום לשנות תלויות בשקט.','- name: Install uv\n  uses: astral-sh/setup-uv@v6\n  with:\n    enable-cache: true\n\n- name: Install Python and dependencies\n  run: uv sync --frozen --dev','ci.yml'),
      section('Run separate understandable gates','מריצים שערים ברורים ונפרדים','Separate steps make the failed responsibility obvious in the GitHub UI. All commands should also work locally so CI is not a mysterious second build system.','Steps נפרדים הופכים את האחריות שנכשלה לברורה ב-GitHub. כל הפקודות צריכות לעבוד גם מקומית כדי ש-CI לא יהיה מערכת Build מסתורית נוספת.','- name: Ruff\n  run: uv run ruff check .\n\n- name: Pylint\n  run: uv run pylint src\n\n- name: Tests\n  run: uv run pytest --cov=src --cov-report=term-missing --cov-fail-under=85','ci.yml'),
      section('CI first, deployment later','קודם CI, אחר כך Deployment','Start with a reliable CI workflow. Add Docker build and cloud deployment only after local containerization works. Protect main and require the CI check before merge.','התחילו ב-Workflow CI אמין. הוסיפו Docker Build ופרסום לענן רק אחרי שה-Container עובד מקומית. הגנו על main ודרשו את בדיקת CI לפני Merge.','test job → docker build job → deploy job\n            needs: test       needs: build','future pipeline')],
    ['Do not use pull_request_target with untrusted code without understanding its privileges','Do not print secrets or enable shell tracing around them','Do not use continue-on-error for required quality gates'],['אל תשתמשו ב-pull_request_target עם קוד לא מהימן בלי להבין הרשאות','אל תדפיסו Secrets ואל תפעילו Shell Tracing סביבם','אל תשתמשו ב-continue-on-error לשערי איכות נדרשים'],
    'Create .github/workflows/ci.yml, deliberately break one test, inspect the failed step, fix it and verify the pull request becomes green.','צרו ‎.github/workflows/ci.yml, שברו בכוונה בדיקה אחת, בדקו איזה Step נכשל, תקנו ואמתו שה-Pull Request הופך ירוק.')
};

function guide(titleEn,titleHe,introEn,introHe,sections,mistakesEn,mistakesHe,exerciseEn,exerciseHe){return{titleEn,titleHe,introEn,introHe,sections,mistakesEn,mistakesHe,exerciseEn,exerciseHe}}
function section(titleEn,titleHe,bodyEn,bodyHe,code='',filename='example'){return{titleEn,titleHe,bodyEn,bodyHe,code,filename}}
