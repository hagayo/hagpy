---
id: 64
slug: cheat-sheets
title: Python cheat sheets
intro: Keep the most useful Python syntax and project commands one click away.
layout: blocks
---

:::explanation title="The core idea"
Keep the most useful Python syntax and project commands one click away.
:::

:::codeExample title="Working example" filename="example.py"
```python
value = items[0] if items else None
unique = set(values)
by_id = {item.id: item for item in items}
with Path("data.txt").open(encoding="utf-8") as file:
    text = file.read()
```
:::

:::checklist title="Key points"
- Organize references by task
- Prefer canonical syntax over tricks
- Show safe defaults beside each command
:::
