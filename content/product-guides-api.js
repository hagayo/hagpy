export const productGuidesApi={
  'service-layer':guide(
    'Why routes should not run the business','למה נתיבים לא צריכים לנהל את העסק',
    'A route understands HTTP. A service understands what the application is trying to do. Separating them keeps rules reusable and tests fast.','נתיב מבין HTTP. ‏Service מבין מה המערכת מנסה לבצע. ההפרדה שומרת את החוקים ניתנים לשימוש חוזר ואת הבדיקות מהירות.',[
      section('The problem with a fat route','הבעיה בנתיב שמן','A beginner often validates input, creates IDs, checks duplicates, writes a file and builds the response inside one route. It works once, but the same use case cannot be called from a script, background job or test without pretending to send HTTP.','מתחילים נוטים לאמת קלט, ליצור מזהים, לבדוק כפילויות, לשמור קובץ ולבנות תגובה בתוך נתיב אחד. זה עובד פעם אחת, אבל אי אפשר להפעיל את אותו תרחיש מסקריפט, Job או בדיקה בלי להעמיד פנים ששולחים HTTP.','@router.post("/dreams")\ndef create_dream(payload: DreamCreate):\n    # Too many responsibilities live here\n    data = load_json()\n    if payload.title in data:\n        raise HTTPException(409)\n    data.append(payload.model_dump())\n    save_json(data)\n    return payload','routes.py'),
      section('The service owns a use case','ה-Service מנהל תרחיש שימוש','A service method receives application data, applies business rules and coordinates dependencies. It does not know whether the caller was FastAPI, a CLI or a test.','מתודת Service מקבלת מידע של המערכת, מפעילה חוקים עסקיים ומתאמת בין תלויות. היא אינה יודעת אם הקורא הוא FastAPI, ‏CLI או בדיקה.','class DreamService:\n    def __init__(self, repository: DreamRepository) -> None:\n        self._repository = repository\n\n    def create(self, command: CreateDream) -> Dream:\n        if self._repository.title_exists(command.title):\n            raise DreamAlreadyExists(command.title)\n\n        dream = Dream.create(command.title, command.description)\n        self._repository.save(dream)\n        return dream','services/dream_service.py'),
      section('The route becomes an adapter','הנתיב הופך למתאם','The route converts the Pydantic request into a command, calls the service, and converts the result into a response model. HTTP errors are mapped at this outer boundary.','הנתיב ממיר את בקשת Pydantic ל-Command, קורא ל-Service וממיר את התוצאה למודל תגובה. שגיאות HTTP ממופות בגבול החיצוני הזה.','@router.post("/dreams", response_model=DreamRead, status_code=201)\ndef create_dream(payload: DreamCreate, service: DreamService = Depends(get_service)):\n    dream = service.create(CreateDream(**payload.model_dump()))\n    return DreamRead.model_validate(dream)','api/dream_routes.py')],
    ['Do not return HTTPException from a service','Do not create one service method for every repository method','Do not turn the service into a bag of unrelated static functions'],['אל תחזירו HTTPException מתוך Service','אל תיצרו מתודת Service עבור כל מתודת Repository','אל תהפכו את ה-Service לאוסף פונקציות סטטיות לא קשורות'],
    'Write a CreateDream service that rejects an empty normalized title and saves exactly once. Test it with a fake repository.','כתבו CreateDream Service שדוחה כותרת ריקה לאחר Normalization ושומר בדיוק פעם אחת. בדקו אותו עם Fake Repository.'),

  'repository-pattern':guide(
    'The application should not know where data lives','המערכת לא צריכה לדעת היכן המידע נשמר',
    'A repository presents persistence as a small collection-like interface. The service depends on that contract, not on JSON, MongoDB or PostgreSQL.','Repository מציג את מנגנון השמירה כממשק קטן שדומה לאוסף. ה-Service תלוי בחוזה הזה, לא ב-JSON, ‏MongoDB או PostgreSQL.',[
      section('Start from what the service needs','מתחילים מהצרכים של ה-Service','Do not copy the entire database API into a repository. Define only operations required by real use cases. A small contract is easier to implement and replace.','אל תעתיקו את כל ה-API של מסד הנתונים ל-Repository. הגדירו רק פעולות שנדרשות מתרחישי שימוש אמיתיים. חוזה קטן קל יותר לממש ולהחליף.','class DreamRepository(Protocol):\n    def save(self, dream: Dream) -> None: ...\n    def get(self, dream_id: UUID) -> Dream | None: ...\n    def list_all(self) -> list[Dream]: ...\n    def title_exists(self, title: str) -> bool: ...','repositories/dream_repository.py'),
      section('A memory implementation for tests','מימוש בזיכרון לבדיקות','The in-memory repository is not a mock. It behaves like a small real storage implementation and makes service tests readable.','Repository בזיכרון אינו Mock. הוא מתנהג כמימוש אחסון אמיתי קטן והופך את בדיקות ה-Service לקריאות.','class InMemoryDreamRepository:\n    def __init__(self) -> None:\n        self._items: dict[UUID, Dream] = {}\n\n    def save(self, dream: Dream) -> None:\n        self._items[dream.id] = dream\n\n    def get(self, dream_id: UUID) -> Dream | None:\n        return self._items.get(dream_id)','tests/fakes/dream_repository.py'),
      section('A skeleton that can grow','שלד שיכול לגדול','Keep domain models, service contracts and infrastructure implementations in separate modules. The dependency arrow points inward: infrastructure implements the application contract.','שמרו Domain Models, חוזי Service ומימושי Infrastructure במודולים נפרדים. חץ התלות פונה פנימה: התשתית מממשת את החוזה של האפליקציה.','src/dreamly/\n├── domain/\n│   └── dream.py\n├── application/\n│   ├── dream_service.py\n│   └── dream_repository.py\n├── infrastructure/\n│   └── json_dream_repository.py\n└── api/\n    ├── dependencies.py\n    └── dream_routes.py','project tree')],
    ['Do not expose database sessions to services','Do not return database rows when the contract promises domain objects','Do not create a generic repository before real needs exist'],['אל תחשפו Database Session ל-Services','אל תחזירו שורות מסד כשהחוזה מבטיח Domain Objects','אל תיצרו Generic Repository לפני שקיים צורך אמיתי'],
    'Implement InMemoryDreamRepository and prove save/get round-trip, missing ID, and replacement behavior with pytest.','ממשו InMemoryDreamRepository והוכיחו באמצעות pytest שמירה וקריאה, מזהה חסר והחלפת ערך.'),

  'secrets-env':guide(
    'A secret is data the code needs but Git must never remember','Secret הוא מידע שהקוד צריך אבל אסור ל-Git לזכור',
    'API keys, passwords and production URLs change between environments and grant access to real resources. They belong in environment configuration, not source code.','API Keys, סיסמאות וכתובות Production משתנים בין סביבות ומעניקים גישה למשאבים אמיתיים. מקומם בהגדרות הסביבה, לא בקוד.',[
      section('Why hard-coded keys are dangerous','למה מפתח בתוך הקוד מסוכן','A Git commit remains in history even after the visible line is deleted. If a real key is committed, treat it as exposed: revoke or rotate it immediately, then remove it from history if necessary.','Commit נשאר בהיסטוריה גם אחרי שמוחקים את השורה הנראית. אם מפתח אמיתי נשמר ב-Git, התייחסו אליו כחשוף: בטלו או החליפו אותו מיד, ורק אחר כך נקו היסטוריה אם צריך.','API_KEY = "sk-real-secret-do-not-do-this"','bad_settings.py'),
      section('.env for local development','‎.env לפיתוח מקומי','A local .env file provides values to your process. It is a convenience file, not encryption. Anyone who can read the file can read the secret.','קובץ ‎.env מקומי מספק ערכים לתהליך. הוא קובץ נוחות, לא הצפנה. כל מי שיכול לקרוא אותו יכול לקרוא את ה-Secret.','APP_ENV=development\nEXTERNAL_API_KEY=replace-with-local-key\nDATABASE_URL=sqlite:///./dreamly.db','.env'),
      section('Ignore the real file, document the shape','מתעלמים מהקובץ האמיתי ומתעדים את המבנה','Commit .env.example with names and safe placeholders so another developer knows what is required. Add .env to .gitignore before the first commit.','שמרו ב-Git את ‎.env.example עם שמות וערכי דוגמה בטוחים כדי שמפתח אחר ידע מה נדרש. הוסיפו ‎.env ל-‎.gitignore לפני ה-Commit הראשון.','.env\n.env.*\n!.env.example\n\n# Python environment\n.venv/','.gitignore'),
      section('Typed settings fail early','הגדרות עם טיפוסים נכשלות מוקדם','pydantic-settings reads environment values and validates them at startup. Missing required configuration should stop the app with a clear error instead of failing during the first user request.','pydantic-settings קורא ערכים מהסביבה ומאמת אותם בעליית המערכת. הגדרה נדרשת שחסרה צריכה לעצור את האפליקציה בשגיאה ברורה במקום להיכשל בבקשת המשתמש הראשונה.','from pydantic import SecretStr\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    external_api_key: SecretStr\n    database_url: str\n    model_config = SettingsConfigDict(env_file=".env", extra="ignore")','settings.py')],
    ['Never log SecretStr.get_secret_value()','Do not put production secrets in .env inside a container image','Adding .env to .gitignore does not remove a file already committed'],['לעולם אל תכתבו ללוג get_secret_value','אל תכניסו Secrets של Production ל-‎.env בתוך Image','הוספת ‎.env ל-‎.gitignore לא מוחקת קובץ שכבר נשמר'],
    'Create .env.example and Settings. Run once without a required key and confirm startup fails clearly, then add the local value and verify success.','צרו ‎.env.example ו-Settings. הריצו פעם אחת ללא מפתח נדרש וודאו שהעלייה נכשלת בבירור, ואז הוסיפו ערך מקומי ואמתו הצלחה.'),

  'frontend-fastapi':guide(
    'Two programs cooperate through one contract','שתי תוכנות משתפות פעולה דרך חוזה אחד',
    'The frontend runs in the browser and manages interaction. FastAPI runs on the server and protects rules and data. HTTP and JSON connect them.','ה-Frontend רץ בדפדפן ומנהל אינטראקציה. FastAPI רץ בשרת ומגן על החוקים והמידע. HTTP ו-JSON מחברים ביניהם.',[
      section('Follow one button click','עוקבים אחרי לחיצה אחת','When the user submits a form, JavaScript reads its fields, builds a request and waits. FastAPI validates the body, calls the service and returns a response. JavaScript then updates the DOM.','כשהמשתמש שולח טופס, JavaScript קורא את השדות, בונה בקשה וממתין. FastAPI מאמת את ה-Body, קורא ל-Service ומחזיר תגובה. לאחר מכן JavaScript מעדכן את ה-DOM.','Submit form\n  ↓\nJavaScript fetch("/api/dreams")\n  ↓\nFastAPI route → Pydantic → Service → Repository\n  ↓\nHTTP response + JSON\n  ↓\nJavaScript updates the page','request flow'),
      section('Define the API contract first','מגדירים קודם את חוזה ה-API','Frontend and backend should agree on URL, method, request JSON, success JSON and error JSON. Swagger helps inspect the contract, but the contract must still be designed.','Frontend ו-Backend צריכים להסכים על URL, מתודה, JSON של הבקשה, JSON של הצלחה ו-JSON של שגיאה. Swagger עוזר לבדוק את החוזה, אבל עדיין צריך לתכנן אותו.','POST /api/dreams\n\nRequest:\n{"title":"Learn Python","description":"Build a real API"}\n\nResponse 201:\n{"id":"8d...","title":"Learn Python","status":"pending"}','API contract'),
      section('One service can serve both sides','שירות אחד יכול להגיש את שני הצדדים','For the first full-stack project, FastAPI can serve index.html and static JavaScript as well as /api routes. This gives one origin, one process and one deployment.','בפרויקט Full-stack הראשון, FastAPI יכול להגיש את index.html ואת JavaScript הסטטי לצד נתיבי ‎/api. כך מקבלים Origin אחד, תהליך אחד ו-Deployment אחד.','app.mount("/static", StaticFiles(directory="static"), name="static")\n\n@app.get("/", include_in_schema=False)\ndef index():\n    return FileResponse("static/index.html")','main.py')],
    ['Do not put business rules only in JavaScript','Do not let the frontend depend on database details','Do not return a different response shape for every error'],['אל תשאירו חוקים עסקיים רק ב-JavaScript','אל תתנו ל-Frontend להיות תלוי בפרטי המסד','אל תחזירו מבנה תגובה שונה לכל שגיאה'],
    'Draw the exact request-response flow for creating one dream, including loading, 201 success, 422 validation and network failure.','ציירו את זרימת הבקשה והתגובה המדויקת ליצירת חלום, כולל Loading, הצלחת 201, שגיאת 422 וכשל רשת.'),

  'fetch-api':guide(
    'fetch sends a request; it does not decide whether the result is good','fetch שולח בקשה, הוא לא מחליט אם התוצאה טובה',
    'fetch returns a Promise for a Response. A 404 or 500 still produces a Response, so code must inspect response.ok or response.status.','fetch מחזיר Promise של Response. גם 404 או 500 מייצרים Response, ולכן הקוד חייב לבדוק response.ok או response.status.',[
      section('The smallest useful GET','בקשת GET השימושית הקטנה ביותר','await pauses only the async function, not the browser. The page can continue painting and responding while the network request is in flight.','await עוצר רק את הפונקציה האסינכרונית, לא את הדפדפן. העמוד יכול להמשיך להצטייר ולהגיב בזמן שהבקשה ברשת.','async function loadDreams() {\n  const response = await fetch("/api/dreams");\n  const dreams = await response.json();\n  return dreams;\n}','app.js'),
      section('Check HTTP before trusting the body','בודקים HTTP לפני שסומכים על ה-Body','fetch rejects on network failures, invalid URLs and similar transport problems. It does not reject merely because the server returned an error status.','fetch זורק שגיאה בכשל רשת, URL שגוי ובעיות תעבורה דומות. הוא אינו זורק רק מפני שהשרת החזיר Status של שגיאה.','async function request(url, options) {\n  const response = await fetch(url, options);\n  const data = response.status === 204 ? null : await response.json();\n\n  if (!response.ok) {\n    throw new ApiError(response.status, data);\n  }\n  return data;\n}','api-client.js'),
      section('A Response body is a stream','ה-Body של Response הוא Stream','Call response.json() once and keep the parsed value. Trying to read the same response body twice causes an error because the stream was already consumed.','קראו ל-response.json() פעם אחת ושמרו את הערך המפוענח. ניסיון לקרוא את אותו Body פעמיים גורם לשגיאה מפני שה-Stream כבר נצרך.','const data = await response.json();\nconsole.log(data);\n// Do not call response.json() again','app.js')],
    ['Do not solve CORS with mode: no-cors','Do not assume fetch rejects on 404','Do not forget to encode user-controlled URL segments'],['אל תפתרו CORS באמצעות mode: no-cors','אל תניחו ש-fetch זורק ב-404','אל תשכחו לקודד חלקי URL שמגיעים מהמשתמש'],
    'Implement requestJson(url, options) that handles 204, parses JSON once and throws an ApiError containing status and body.','ממשו requestJson(url, options) שמטפל ב-204, מפענח JSON פעם אחת וזורק ApiError שמכיל Status ו-Body.'),

  'json-data':guide(
    'JSON is a text format crossing the network','JSON הוא פורמט טקסט שחוצה את הרשת',
    'Python dictionaries and JavaScript objects exist in memory. JSON is their serialized text representation, with a smaller set of supported value types.','מילוני Python ואובייקטי JavaScript קיימים בזיכרון. JSON הוא ייצוג הטקסט שלהם לצורך העברה, עם אוסף מצומצם יותר של טיפוסים.',[
      section('The six JSON value kinds','ששת סוגי הערכים של JSON','JSON supports object, array, string, number, boolean and null. It does not directly support UUID, datetime, Decimal, set or Python class instances.','JSON תומך ב-Object, ‏Array, ‏String, ‏Number, ‏Boolean ו-null. הוא אינו תומך ישירות ב-UUID, ‏datetime, ‏Decimal, ‏set או מופעי מחלקות Python.','{\n  "id": "73f2...",\n  "title": "Learn FastAPI",\n  "steps": ["model", "route", "test"],\n  "done": false,\n  "completed_at": null\n}','response.json'),
      section('Serialization and parsing','Serialization ו-Parsing','FastAPI serializes response models to JSON. In the browser, response.json() parses JSON text into JavaScript values. JSON.stringify performs the opposite direction for a request body.','FastAPI מבצע Serialization של מודלי תגובה ל-JSON. בדפדפן response.json() מפענח טקסט JSON לערכי JavaScript. ‏JSON.stringify מבצע את הכיוון ההפוך עבור Body של בקשה.','const payload = { title: "Learn FastAPI" };\n\nfetch("/api/dreams", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify(payload),\n});','app.js'),
      section('Pydantic controls the boundary','Pydantic שולט בגבול','Use Pydantic response models to make output predictable and prevent accidental exposure of internal or secret fields.','השתמשו במודלי תגובה של Pydantic כדי להפוך את הפלט לצפוי ולמנוע חשיפה מקרית של שדות פנימיים או סודיים.','class DreamRead(BaseModel):\n    id: UUID\n    title: str\n    status: DreamStatus\n    completed_at: datetime | None','schemas.py')],
    ['JSON object keys must use double quotes','Do not manually concatenate JSON strings','Do not send a Python repr and call it JSON'],['מפתחות JSON חייבים גרשיים כפולים','אל תחברו מחרוזות JSON ידנית','אל תשלחו Python repr ותקראו לו JSON'],
    'For a DreamRead object, write the exact JSON response and identify which Python values must be converted to strings.','עבור אובייקט DreamRead, כתבו את תגובת ה-JSON המדויקת וזהו אילו ערכי Python חייבים להפוך למחרוזות.'),

  'http-crud-methods':guide(
    'The HTTP method describes the intent','מתודת HTTP מתארת את הכוונה',
    'CRUD is a useful mapping, but HTTP semantics are the real contract. The same URL can support different actions through different methods.','CRUD הוא מיפוי שימושי, אבל המשמעות של HTTP היא החוזה האמיתי. אותו URL יכול לתמוך בפעולות שונות באמצעות מתודות שונות.',[
      section('GET reads without changing state','GET קורא בלי לשנות מצב','GET should be safe: refreshing or retrying it must not complete a task, send an email or delete data. Query parameters filter or paginate the result.','GET צריך להיות Safe: רענון או ניסיון חוזר לא צריכים להשלים משימה, לשלוח אימייל או למחוק מידע. Query Parameters מסננים או מדפדפים בתוצאה.','const dreams = await requestJson("/api/dreams?status=pending&limit=20");','app.js'),
      section('POST creates a new resource','POST יוצר משאב חדש','Send JSON, declare Content-Type and return 201 with the created representation or its location. Repeating the same POST may create another resource unless idempotency is designed.','שולחים JSON, מצהירים Content-Type ומחזירים 201 עם הייצוג שנוצר או מיקומו. חזרה על אותו POST עשויה ליצור משאב נוסף אם לא תוכננה Idempotency.','await requestJson("/api/dreams", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ title, description }),\n});','app.js'),
      section('PATCH changes selected fields','PATCH משנה שדות נבחרים','PATCH expresses a partial update. Pydantic fields are optional in the update schema, and model_dump(exclude_unset=True) distinguishes missing values from explicit null.','PATCH מבטא עדכון חלקי. שדות Pydantic אופציונליים בסכמת העדכון, ו-model_dump(exclude_unset=True) מבדיל בין ערך חסר ל-null מפורש.','class DreamUpdate(BaseModel):\n    title: str | None = None\n    status: DreamStatus | None = None\n\nchanges = payload.model_dump(exclude_unset=True)','schemas.py'),
      section('DELETE removes and often returns 204','DELETE מוחק ולעיתים מחזיר 204','A successful delete can return 204 with no response body. The frontend must not call response.json() for that response.','מחיקה מוצלחת יכולה להחזיר 204 ללא Response Body. ה-Frontend אינו צריך לקרוא response.json() עבור תגובה כזו.','await requestJson(`/api/dreams/${encodeURIComponent(id)}`, {\n  method: "DELETE",\n});','app.js')],
    ['Do not use POST for every action','Do not send a request body with GET','Do not treat PUT and PATCH as identical'],['אל תשתמשו ב-POST לכל פעולה','אל תשלחו Body עם GET','אל תתייחסו ל-PUT ול-PATCH כזהים'],
    'Implement createDream, updateDreamStatus and deleteDream using one requestJson helper. State the expected success status for each.','ממשו createDream, ‏updateDreamStatus ו-deleteDream באמצעות requestJson אחד. ציינו את Status ההצלחה הצפוי לכל פעולה.'),

  'frontend-loading-errors':guide(
    'A network feature has more than success and failure','לפיצ׳ר רשת יש יותר מהצלחה וכישלון',
    'A usable interface represents idle, loading, success, empty and error states. If one is missing, the user eventually sees a frozen button, stale data or silence.','ממשק שימושי מייצג Idle, ‏Loading, הצלחה, תוצאה ריקה ושגיאה. אם מצב אחד חסר, המשתמש יראה בסוף כפתור תקוע, מידע ישן או שתיקה.',[
      section('Model the state explicitly','מייצגים את המצב במפורש','Do not infer loading from whether a list happens to be empty. Empty data can be a successful result. Store status and data separately.','אל תסיקו Loading מכך שרשימה במקרה ריקה. מידע ריק יכול להיות תוצאה מוצלחת. שמרו Status ו-Data בנפרד.','const state = {\n  status: "idle", // idle | loading | success | error\n  dreams: [],\n  error: null,\n};','state.js'),
      section('Always leave loading in finally','תמיד יוצאים מ-Loading ב-finally','Disable the submit button before the request and restore it in finally. This runs after success or failure and prevents the interface from becoming permanently disabled.','נטרלו את כפתור השליחה לפני הבקשה והחזירו אותו ב-finally. הקטע הזה רץ אחרי הצלחה או כישלון ומונע מהממשק להישאר מנוטרל.','submitButton.disabled = true;\nsetStatus("loading");\ntry {\n  await createDream(readForm());\n  setStatus("success");\n} catch (error) {\n  showError(toUserMessage(error));\n  setStatus("error");\n} finally {\n  submitButton.disabled = false;\n}','dream-form.js'),
      section('Errors must suggest the next action','שגיאה צריכה להציע את הפעולה הבאה','A network failure may invite Retry. A 422 should point to fields. A 409 explains a conflict. A 500 apologizes and provides a safe retry path without exposing a traceback.','כשל רשת יכול להציע ניסיון חוזר. 422 מצביע על שדות. 409 מסביר התנגשות. 500 מתנצל ומאפשר ניסיון בטוח בלי לחשוף Traceback.','if (error.status === 422) showFieldErrors(error.body.detail);\nelse if (error.status === 409) showMessage("A dream with this title already exists");\nelse showMessage("Could not save. Try again.");','errors.js')],
    ['Do not replace the whole page with a spinner','Do not show raw exception text to end users','Do not clear user input after a failed submit'],['אל תחליפו את כל העמוד ב-Spinner','אל תציגו טקסט Exception גולמי למשתמש','אל תמחקו קלט משתמש אחרי שליחה שנכשלה'],
    'Design a state table for loading a dream list and submitting a new dream. Include the visible UI and enabled actions in each state.','תכננו טבלת מצבים לטעינת רשימת חלומות ולשליחת חלום חדש. כללו את ה-UI הנראה ואת הפעולות המותרות בכל מצב.'),

  'http-status-codes':guide(
    'A status code summarizes the outcome','Status Code מסכם את התוצאה',
    'The code tells clients which category of result occurred. The JSON body adds details. Returning 200 for every outcome forces every client to invent its own interpretation.','הקוד מספר ללקוחות איזו קטגוריית תוצאה התרחשה. ה-JSON מוסיף פרטים. החזרת 200 לכל מצב מאלצת כל לקוח להמציא פרשנות משלו.',[
      section('Success codes are different promises','קודי הצלחה הם הבטחות שונות','200 includes a normal response. 201 confirms creation. 204 confirms success with no body. The frontend should handle each deliberately.','200 כולל תגובה רגילה. 201 מאשר יצירה. 204 מאשר הצלחה ללא Body. ה-Frontend צריך לטפל בכל אחד במכוון.','200 OK          GET /api/dreams/{id}\n201 Created     POST /api/dreams\n204 No Content  DELETE /api/dreams/{id}','HTTP'),
      section('Client-visible request problems','בעיות בבקשה שנראות ללקוח','400 is a malformed or invalid request not covered by a more specific code. 401 means authentication is required or invalid. 403 means identity is known but not allowed. 404 means the resource is absent. 409 means current state conflicts. 422 is FastAPI validation failure.','400 היא בקשה פגומה שאין לה קוד מדויק יותר. 401 אומר שנדרש Authentication או שהוא נכשל. 403 אומר שהזהות ידועה אך אינה מורשית. 404 אומר שהמשאב חסר. 409 מציין התנגשות מצב. 422 היא שגיאת Validation של FastAPI.','400 Bad Request\n401 Unauthorized\n403 Forbidden\n404 Not Found\n409 Conflict\n422 Unprocessable Content','HTTP'),
      section('Server failures are not user mistakes','כשל שרת אינו טעות משתמש','500 means an unexpected server failure. Log the full exception with a correlation ID, but return a stable safe body. Do not convert every exception into 400.','500 מציין כשל שרת בלתי צפוי. כתבו את ה-Exception המלא ללוג עם Correlation ID, אבל החזירו Body יציב ובטוח. אל תהפכו כל Exception ל-400.','{\n  "code": "internal_error",\n  "message": "The request could not be completed",\n  "request_id": "req_8f31"\n}','500 response')],
    ['401 is named Unauthorized but usually means unauthenticated','A 404 should not reveal private resource existence when security forbids it','Do not return a body with 204'],['401 נקרא Unauthorized אך בדרך כלל מציין Unauthenticated','404 לא צריך לחשוף קיום משאב פרטי כשאבטחה אוסרת זאת','אל תחזירו Body יחד עם 204'],
    'For create, read missing, duplicate title, invalid body, forbidden update and unexpected database failure, choose a status and explain the contract.','עבור יצירה, קריאת משאב חסר, כותרת כפולה, Body לא תקין, עדכון אסור וכשל מסד בלתי צפוי, בחרו Status והסבירו את החוזה.'),

  'pydantic-validation-errors':guide(
    'FastAPI already tells us where validation failed','FastAPI כבר מספר לנו היכן ה-Validation נכשל',
    'A 422 response contains a detail array. Each item identifies the location, error message, error type and sometimes the rejected input. The frontend can turn it into field-level feedback.','תגובת 422 מכילה מערך detail. כל פריט מזהה מיקום, הודעה, סוג שגיאה ולעיתים את הקלט שנדחה. ה-Frontend יכול להפוך אותו למשוב ליד השדה.',[
      section('Read the error shape','קוראים את מבנה השגיאה','loc usually starts with body, query or path. For a request model field, the remaining location segments identify the field, including nested objects and list indexes.','loc מתחיל בדרך כלל ב-body, ‏query או path. עבור שדה במודל בקשה, שאר חלקי המיקום מזהים את השדה, כולל אובייקטים מקוננים ואינדקסים ברשימה.','{\n  "detail": [{\n    "type": "string_too_short",\n    "loc": ["body", "title"],\n    "msg": "String should have at least 3 characters",\n    "input": "AI"\n  }]\n}','422 response'),
      section('Map errors to form controls','ממפים שגיאות לשדות הטופס','Create a predictable field key from loc, find the matching form control and place a safe message beside it. Unknown locations go to a form-level summary instead of disappearing.','צרו מפתח שדה צפוי מתוך loc, מצאו את רכיב הטופס המתאים והציגו לידו הודעה בטוחה. מיקומים לא מוכרים עוברים לסיכום ברמת הטופס במקום להיעלם.','function fieldFromLocation(location) {\n  return location.filter(part => part !== "body").join(".");\n}\n\nfor (const error of detail) {\n  showFieldError(fieldFromLocation(error.loc), error.msg);\n}','validation-errors.js'),
      section('Keep server validation authoritative','שומרים את אימות השרת כסמכות','HTML validation improves speed and usability, but it can be bypassed. Pydantic remains the trusted boundary because requests can come from scripts, other clients or modified browser code.','HTML Validation משפר מהירות ושימושיות, אך אפשר לעקוף אותו. Pydantic נשאר הגבול המהימן מפני שבקשות יכולות להגיע מסקריפטים, Clients אחרים או קוד דפדפן ששונה.','<input name="title" minlength="3" maxlength="80" required>\n\nclass DreamCreate(BaseModel):\n    title: str = Field(min_length=3, max_length=80)','frontend + backend')],
    ['Do not show the complete raw 422 response in the page','Do not translate errors by fragile full-message matching','Do not trust frontend validation for security'],['אל תציגו את תגובת 422 הגולמית המלאה בעמוד','אל תתרגמו שגיאות באמצעות התאמה שבירה של ההודעה כולה','אל תסמכו על Validation ב-Frontend לצורכי אבטחה'],
    'Given errors for title, steps.0.description and a query parameter, map each to either a field message or form summary.','עבור שגיאות ב-title, ‏steps.0.description ו-Query Parameter, מפו כל אחת להודעת שדה או לסיכום הטופס.'),

  'same-origin-cors':guide(
    'Origin means scheme, host and port together','Origin הוא שילוב של Scheme, ‏Host ו-Port',
    'Browsers protect pages from reading responses from another origin unless that server explicitly allows it. CORS is a browser permission protocol, not authentication.','דפדפנים מגינים על עמודים מקריאת תגובות מ-Origin אחר, אלא אם השרת מאפשר זאת במפורש. CORS הוא פרוטוקול הרשאה לדפדפן, לא Authentication.',[
      section('Recognize different origins','מזהים Origins שונים','Changing http to https, localhost to 127.0.0.1 or port 8000 to 5500 creates a different origin. Paths do not affect origin.','שינוי מ-http ל-https, מ-localhost ל-127.0.0.1 או מ-Port 8000 ל-5500 יוצר Origin אחר. נתיבים אינם משפיעים על Origin.','http://localhost:8000/app\nhttp://localhost:8000/api   # same origin\nhttp://localhost:5500      # different port\nhttps://localhost:8000     # different scheme\nhttp://127.0.0.1:8000      # different host','origins'),
      section('Preflight asks permission first','Preflight מבקש רשות קודם','For many cross-origin requests, the browser sends OPTIONS with the intended method and headers. If the response does not permit them, the browser stops before exposing the real response to JavaScript.','עבור בקשות Cross-origin רבות, הדפדפן שולח OPTIONS עם המתודה וה-Headers המתוכננים. אם התגובה אינה מאשרת אותם, הדפדפן עוצר לפני חשיפת התגובה האמיתית ל-JavaScript.','OPTIONS /api/dreams\nOrigin: http://localhost:5500\nAccess-Control-Request-Method: POST\nAccess-Control-Request-Headers: content-type','preflight'),
      section('Configure exact development origins','מגדירים Origins מדויקים לפיתוח','FastAPI CORSMiddleware should list trusted frontend origins. Wildcard is inappropriate for private APIs and cannot be combined with credentialed requests in the normal way.','FastAPI CORSMiddleware צריך לרשום Origins מהימנים של ה-Frontend. Wildcard אינו מתאים ל-API פרטי ואינו משתלב באופן רגיל עם בקשות שכוללות Credentials.','app.add_middleware(\n    CORSMiddleware,\n    allow_origins=["http://localhost:5500"],\n    allow_credentials=False,\n    allow_methods=["GET", "POST", "PATCH", "DELETE"],\n    allow_headers=["Content-Type"],\n)','main.py'),
      section('Prefer same origin for the first deployment','מעדיפים Same Origin בפרסום הראשון','Serving frontend and API from the same FastAPI service avoids CORS entirely. Split them later when the architectural benefit justifies the extra configuration.','הגשת ה-Frontend וה-API מאותו FastAPI Service מונעת CORS לחלוטין. פצלו אותם בהמשך כאשר היתרון הארכיטקטוני מצדיק את ההגדרה הנוספת.')],
    ['mode: no-cors does not fix readable API responses','CORS does not protect non-browser clients','Allowing every origin is not authentication'],['mode: no-cors אינו פותר קריאת תגובות API','CORS אינו מגן מול Clients שאינם דפדפן','אישור כל Origin אינו Authentication'],
    'For frontend localhost:5500 and API localhost:8000, write the exact allowed origin and explain why localhost:3000 is still blocked.','עבור Frontend ב-localhost:5500 ו-API ב-localhost:8000, כתבו את ה-Origin המדויק שמותר והסבירו למה localhost:3000 עדיין חסום.')
};

function guide(titleEn,titleHe,introEn,introHe,sections,mistakesEn,mistakesHe,exerciseEn,exerciseHe){return{titleEn,titleHe,introEn,introHe,sections,mistakesEn,mistakesHe,exerciseEn,exerciseHe}}
function section(titleEn,titleHe,bodyEn,bodyHe,code='',filename='example'){return{titleEn,titleHe,bodyEn,bodyHe,code,filename}}
